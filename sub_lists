def sublists(ls):
    subs = [[]]
    for i in range(len(ls) + 1):
        for j in range(i+1, len(ls)+1):
            sub = ls[i:j]
            subs.append(sub)

    return subs

print(sublists([1,2,3,4]))
